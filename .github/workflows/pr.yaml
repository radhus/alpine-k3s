name: pr
on: [pull_request]
jobs:
  build-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Build image
        run: |
          make EMAIL=test@test.test genkeys
          make build
